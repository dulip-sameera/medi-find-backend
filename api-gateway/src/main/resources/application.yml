spring:
  application:
    name: api-gateway
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"

# Basic actuator for health checks
management:
  endpoints:
    web:
      exposure:
        include: health